{
  "name": "ghostchat",
  "version": "1.0.0",
  "description": "Ephemeral private chat rooms. No accounts. No logs. No trace.",
  "main": "server/server.js",
  "scripts": {
    "dev": "concurrently \"nodemon server/server.js\" \"cd client && npm run dev\"",
    "start": "node server/server.js",
    "build": "cd client && npm run build",
    "install:all": "npm install && cd client && npm install"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.3.1",
    "helmet": "^7.1.0",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.1.3"
  }
}
